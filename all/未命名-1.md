```mermaid
graph TD
    %% 状态机主流程
    START((开始)) --> INIT[初始化: LCD, 密码加载, 电机归位, 红外];
    INIT --> S0[state=0: 显示输入提示];

    
    S3 --> POST_KEY{读取按键};
    
    POST_KEY -- NO --> S3;
    
    POST_KEY -- *(10) --> S3_CLOSE[关锁: 电机, 播放音乐];
    S3_CLOSE --> S0;
    
    POST_KEY -- #(12) --> S4[state=4: 密码设置模式 (进入子循环)];

    
```