```mermaid
graph TD
    %% 状态机主流程
    START((开始)) --> INIT[初始化: LCD, 密码加载, 电机归位, 红外];
    INIT --> S0[state=0: 显示输入提示];

    %% 状态 1: 密码输入与验证
    S0 --> S1[state=1: 输入等待 (*=确认, #=删除)];
    


   
   
```